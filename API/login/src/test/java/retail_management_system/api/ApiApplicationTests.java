package retail_management_system.api;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@Disable("This test is disabled because it only checks if the application context loads successfully, which is not necessary for our current testing strategy.")
@SpringBootTest
class ApiApplicationTests {

	@Test
	void contextLoads() {
	}

}
